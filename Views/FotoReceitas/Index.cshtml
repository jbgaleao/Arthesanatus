@model IEnumerable<Arthesanatus.Models.FotoReceita>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Receita.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CaminhoFoto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Receita.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CaminhoFoto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FotoReceitaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FotoReceitaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FotoReceitaId })
        </td>
    </tr>
}

</table>
